# Copy to `.env.local` (CRA) and fill in values matching the relayer setup.
# Note: REACT_APP_RELAYER_API_KEY is exposed to the browser. See ../relayer/README.md
# for the security tradeoffs (whitelist_receivers + on-chain sig check + rate limit).

REACT_APP_RELAYER_URL=http://localhost:8080
REACT_APP_RELAYER_ID=polkagreet
REACT_APP_RELAYER_API_KEY=
